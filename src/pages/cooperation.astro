---
import type { Lang } from '@/i18n';
import { getBaseUrl } from '@/i18n';
import BaseLayout from '@/layouts/BaseLayout.astro';
import Breadcrumbs from '@/components/Breadcrumbs.astro';
import CtaFaq from '@/components/CtaFaq.astro';
import Faq from '@/components/Faq.astro';
import Partners from '@/components/Partners.astro';

const lang: Lang = 'ru';
const base = getBaseUrl(lang);

const faqItems = [
  {
    question: "Какой минимальный авансовый платёж?",
    answer: "Минимальный авансовый платёж составляет всего 15% от стоимости контракта. Также возможен отложенный платёж до 30% от стоимости до момента ввода оборудования в эксплуатацию."
  },
  {
    question: "Есть ли возможность рассрочки?",
    answer: "Да, для приоритетных проектов доступна программа рассрочки на 12 месяцев на 50% стоимости контракта."
  },
  {
    question: "Какие сроки выполнения заказа?",
    answer: "Сроки устанавливаются индивидуально, однако согласно общим условиям заказ будет завершён в течение 5 месяцев с момента получения полной оплаты по договору."
  },
  {
    question: "Предоставляется ли гарантия на оборудование?",
    answer: "Да, на всю поставляемую продукцию действует гарантия от производителя или сервисный договор. Сроки и условия уточняются в каждом конкретном случае."
  }
];

const conditions = [
  {
    title: 'Сроки выполнения заказа',
    description: 'Сроки выполнения заказа устанавливаются с учетом индивидуальных особенностей, однако, согласно общим условиям, заказ будет завершён в течение 5 месяцев с момента получения полной оплаты по договору.',
    icon: '/img/cooperation/order_times.png'
  },
  {
    title: 'Гарантия',
    description: 'На всю поставляемую продукцию действует гарантия от производителя или сервисный договор. Сроки и условия можно уточнить в каждом конкретном случае.',
    icon: '/img/cooperation/warranty.png'
  },
  {
    title: 'Сертификация',
    description: 'При поставке оборудования, требующего сертификации, предоставляем копии необходимых сертификатов и паспортов качества.',
    icon: '/img/cooperation/certification.png'
  },
  {
    title: 'Контактная информация',
    description: '',
    icon: '/img/cooperation/contacts.png',
    contacts: [
      { text: 'Тел: 8(916) 111-4-555' },
      { text: 'Email: office@rustrade.pro' },
      { text: 'г. Пермь, ул.Олега Кошевого, д.31, пом.20.' },
      { text: 'г. Москва, Ленинский проспект, д.109, оф.35.' },
      { text: 'Понедельник – Пятница, 9:00 – 18:00' }
    ]
  }
];
---

<BaseLayout 
  lang={lang} 
  title="Условия сотрудничества | РУСТРЕЙД"
  description="Условия оплаты и доставки оборудования. Авансовый платёж от 15%, рассрочка на 12 месяцев, гарантия от производителя."
  keywords="условия сотрудничества, оплата, доставка, рассрочка, лизинг, гарантия, РУСТРЕЙД"
>
  <Breadcrumbs items={[{ label: 'Условия сотрудничества' }]} />

  <!-- Main Payment Block -->
  <section class="payment-block">
    <div class="payment-image">
      <img src="/img/cooperation/oplata_3.webp" alt="Оплата и доставка" />
      <div class="image-overlay-bottom">
        <h2>Способы оплаты</h2>
        <p>Мы ценим ваше время и стремимся обеспечить удобные условия для сотрудничества.</p>
      </div>
    </div>
    <div class="payment-info">
      <div class="info-block">
        <div class="info-header">
          <div class="info-icon">
            <img src="/img/cooperation/beznal_icon.png" alt="Безналичный расчёт" />
          </div>
          <span class="info-title">Всё об оплате</span>
        </div>
        <p>В ответ на запросы рынка нами разработаны оптимальные условия оплаты для удобства и комфорта заказчика</p>
        <ul class="info-benefits">
          <li>Авансовый платеж всего от 15% от стоимости контракта</li>
          <li>Возможность предоставления отложенного платежа до момента ввода оборудования в эксплуатацию в размере до 30% от стоимости контракта</li>
        </ul>
      </div>
      <div class="info-block">
        <div class="info-header">
          <div class="info-icon">
            <img src="/img/cooperation/lizing.png" alt="Лизинг / Рассрочка" />
          </div>
          <span class="info-title">Рассрочка</span>
        </div>
        <ul class="info-benefits">
          <li>Наличие программы стимулирования спроса в части предоставления рассрочки на 12 месяцев на 50% стоимости контракта для проектов максимальной приоритетности для Компании</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- Conditions Grid -->
  <section class="conditions-section">
    <div class="conditions-grid">
      {conditions.map(condition => (
        <div class="condition-card">
          <div class="condition-header">
            <img src={condition.icon} alt={condition.title} class="condition-icon" />
            <span class="condition-title">{condition.title}</span>
          </div>
          {condition.description && (
            <ul class="condition-benefits">
              <li>{condition.description}</li>
            </ul>
          )}
          {condition.contacts && (
            <ul class="condition-benefits">
              {condition.contacts.map(contact => (
                <li>{contact.text}</li>
              ))}
            </ul>
          )}
        </div>
      ))}
    </div>
  </section>

  <!-- Delivery Block -->
  <section class="delivery-block">
    <div class="delivery-image">
      <img src="/img/cooperation/Dostavka_3.webp" alt="Доставка" />
      <div class="delivery-overlay">
        <h2>Условия и способы доставки</h2>
        <div class="delivery-details">
          <div class="delivery-section">
            <h3>Доставка Ж/Д транспортом</h3>
            <p>При транспортировке железнодорожным транспортом мы осуществляем контроль за погрузкой и качеством крепления на платформе.</p>
          </div>
          <div class="delivery-section">
            <h3>Собственная логистика</h3>
            <p>Мы можем осуществить доставку оборудования своими силами. Наш подход обеспечивает оперативную, безопасную и надежную транспортировку, а также своевременное прибытие техники в идеальном состоянии.</p>
          </div>
          <div class="delivery-section">
            <h3>Доставка комплектующих и запасных частей</h3>
            <p>Оборудование, запчасти и комплектующие могут быть отправлены через транспортные компании, при этом оплата автоперевозки производится согласно действующим тарифам перевозчиков.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Partners Section -->
  <Partners />

  <Faq items={faqItems} />

  <CtaFaq />
</BaseLayout>

<style>
  /* Payment Block */
  .payment-block {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-6);
    margin-bottom: var(--space-8);
  }

  .payment-image {
    position: relative;
    border-radius: var(--radius-xl);
    overflow: hidden;
    min-height: 500px;
  }

  .payment-image > img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    position: absolute;
    top: 0;
    left: 0;
  }

  .image-overlay-top {
    position: absolute;
    top: var(--space-4);
    left: var(--space-4);
    display: flex;
    align-items: center;
    gap: var(--space-2);
    background: rgba(255, 255, 255, 0.95);
    padding: var(--space-2) var(--space-4);
    border-radius: var(--radius-full);
  }

  .circle-icon img {
    width: 20px;
    height: 20px;
  }

  .image-overlay-top span {
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--color-primary);
  }

  .image-overlay-bottom {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: var(--space-6);
    background: linear-gradient(to top, rgba(0, 0, 0, 0.8), transparent);
  }

  .image-overlay-bottom h2 {
    font-size: var(--text-2xl);
    color: var(--color-white);
    margin-bottom: var(--space-2);
  }

  .image-overlay-bottom p {
    font-size: var(--text-base);
    color: rgba(255, 255, 255, 0.9);
    line-height: 1.6;
  }

  .payment-info {
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .info-block {
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border);
  }

  .info-block:last-child {
    border-bottom: none;
    padding-bottom: 0;
  }

  .info-header {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-4);
  }

  .info-icon {
    width: 48px;
    height: 48px;
    background: linear-gradient(135deg, #c5dff0 0%, #a8d4ed 100%);
    border-radius: var(--radius-lg);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-2);
  }

  .info-icon img {
    width: 28px;
    height: 28px;
    object-fit: contain;
    filter: brightness(0);
  }

  .info-title {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-primary);
  }

  .info-block > p {
    font-size: var(--text-base);
    color: var(--color-text-muted);
    line-height: 1.6;
    margin-bottom: var(--space-4);
  }

  .info-benefits {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .info-benefits li {
    position: relative;
    padding-left: var(--space-5);
    font-size: var(--text-base);
    color: var(--color-text-muted);
    line-height: 1.6;
    margin-bottom: var(--space-3);
  }

  .info-benefits li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 8px;
    width: 8px;
    height: 8px;
    background: var(--color-accent);
    border-radius: 50%;
  }

  /* Conditions Section */
  .conditions-section {
    margin-bottom: var(--space-8);
  }

  .conditions-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-4);
  }

  .condition-card {
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--space-5);
  }

  .condition-header {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-4);
  }

  .condition-icon {
    width: 40px;
    height: 40px;
    object-fit: contain;
  }

  .condition-title {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-primary);
  }

  .condition-benefits {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .condition-benefits li {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    line-height: 1.6;
    margin-bottom: var(--space-2);
  }

  /* Delivery Block */
  .delivery-block {
    margin-bottom: var(--space-8);
  }

  .delivery-image {
    position: relative;
    border-radius: var(--radius-xl);
    overflow: hidden;
    min-height: 450px;
  }

  .delivery-image > img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    position: absolute;
    top: 0;
    left: 0;
  }

  .delivery-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: var(--space-8);
    background: linear-gradient(to top, rgba(0, 0, 0, 0.9) 0%, rgba(0, 0, 0, 0.7) 50%, transparent 100%);
  }

  .delivery-overlay h2 {
    font-size: var(--text-2xl);
    color: var(--color-white);
    margin-bottom: var(--space-6);
  }

  .delivery-details {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
  }

  .delivery-section h3 {
    font-size: var(--text-base);
    color: var(--color-white);
    margin-bottom: var(--space-2);
  }

  .delivery-section p {
    font-size: var(--text-sm);
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.6;
  }

  /* CTA Section */
  .cta {
    background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
    border-radius: var(--radius-xl);
    padding: var(--space-10);
    margin-bottom: var(--space-10);
  }

  .cta-content {
    text-align: center;
  }

  .cta-content h2 {
    font-size: var(--text-2xl);
    margin-bottom: var(--space-3);
    color: var(--color-primary);
  }

  .cta-content p {
    font-size: var(--text-base);
    color: var(--color-text-muted);
    margin-bottom: var(--space-6);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  @media (max-width: 1200px) {
    .conditions-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 1024px) {
    .payment-block {
      grid-template-columns: 1fr;
    }

    .payment-image {
      min-height: 350px;
    }

    .delivery-details {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }
  }

  @media (max-width: 768px) {
    .conditions-grid {
      grid-template-columns: 1fr;
    }

    .delivery-image {
      min-height: 600px;
    }

    .delivery-overlay {
      padding: var(--space-5);
    }
  }
</style>
