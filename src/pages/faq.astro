---
import type { Lang } from '@/i18n';
import { t, getBaseUrl } from '@/i18n';
import BaseLayout from '@/layouts/BaseLayout.astro';
import Breadcrumbs from '@/components/Breadcrumbs.astro';

const lang: Lang = 'ru';
const base = getBaseUrl(lang);

const faqItems = [
  {
    question: "Какие сроки поставки оборудования?",
    answer: "Сроки поставки зависят от типа оборудования и сложности проекта. Стандартные позиции могут быть отгружены в течение 2-4 недель, в то время как сложные индивидуальные заказы могут требовать от 4 до 12 месяцев на производство и поставку. Мы всегда предоставляем точный график при согласовании заказа."
  },
  {
    question: "Предоставляете ли вы гарантию на оборудование?",
    answer: "Да, на все поставляемое нами оборудование распространяется гарантия. Стандартный гарантийный срок составляет 12 месяцев с момента ввода в эксплуатацию, но не более 18 месяцев с момента поставки. Возможно расширение гарантии по согласованию."
  },
  {
    question: "Осуществляете ли вы монтаж и пусконаладочные работы?",
    answer: "Да, наша компания предлагает полный комплекс услуг, включая шеф-монтаж, монтаж и пусконаладочные работы. Наши специалисты имеют большой опыт работы с энергетическим оборудованием и гарантируют качественное выполнение работ."
  },
  {
    question: "Работаете ли вы с проектами «под ключ»?",
    answer: "Да, мы специализируемся на реализации проектов «под ключ». Мы берем на себя все этапы: от проектирования и подбора оборудования до поставки, монтажа и ввода в эксплуатацию."
  },
  {
    question: "Какие условия оплаты вы предлагаете?",
    answer: "Мы предлагаем гибкие условия оплаты, которые обсуждаются индивидуально с каждым заказчиком. Возможна поэтапная оплата, лизинг и другие финансовые инструменты."
  },
  {
    question: "Где находится ваше производство?",
    answer: "Мы сотрудничаем с ведущими производственными площадками в России и за рубежом. Наши партнеры проходят строгий контроль качества, чтобы обеспечить соответствие оборудования всем необходимым стандартам."
  }
];
---

<BaseLayout 
  lang={lang} 
  title="Ответы на вопросы | РУСТРЕЙД"
  description="Часто задаваемые вопросы о работе компании РУСТРЕЙД, поставках оборудования и услугах."
>
  <Breadcrumbs items={[{ label: 'Ответы на вопросы' }]} />

  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-content">
      <h1>Ответы на вопросы</h1>
      <p>Здесь вы найдете ответы на самые популярные вопросы о нашей работе, оборудовании и услугах.</p>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="section faq-section">
    <div class="faq-container">
      {faqItems.map((item, index) => (
        <div class="faq-item">
          <button class="faq-question" aria-expanded="false">
            <span>{item.question}</span>
            <div class="faq-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M12 5V19M5 12H19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </div>
          </button>
          <div class="faq-answer">
            <div class="faq-answer-content">
              <p>{item.answer}</p>
            </div>
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- CTA Section -->
  <section class="section cta">
    <div class="cta-content">
      <h2>Не нашли ответ на свой вопрос?</h2>
      <p>Свяжитесь с нами, и наши специалисты проконсультируют вас по любым вопросам.</p>
      <button class="btn btn-accent" data-modal="contact">
        <span>Задать вопрос</span>
        <img src="/img/message.svg" alt="" width="16" height="16" />
      </button>
    </div>
  </section>
</BaseLayout>

<script>
  function initAccordion() {
    const questions = document.querySelectorAll('.faq-question');
    
    questions.forEach(question => {
      question.addEventListener('click', () => {
        const expanded = question.getAttribute('aria-expanded') === 'true';
        
        // Close all other items
        questions.forEach(q => {
          if (q !== question) {
            q.setAttribute('aria-expanded', 'false');
            q.nextElementSibling?.classList.remove('open');
          }
        });

        // Toggle current item
        question.setAttribute('aria-expanded', (!expanded).toString());
        question.nextElementSibling?.classList.toggle('open');
      });
    });
  }

  document.addEventListener('astro:page-load', initAccordion);
  document.addEventListener('DOMContentLoaded', initAccordion);
</script>

<style>
  /* Common Styles */
  .section {
    margin-bottom: var(--space-12);
  }

  /* Hero Section */
  .hero {
    background: linear-gradient(59deg, rgba(42, 45, 46, 0.85) 0%, rgba(0, 0, 0, 0.60) 31.59%, rgba(42, 45, 46, 0.85) 100%), 
                url('/img/main/drive.webp') center/cover no-repeat;
    border-radius: var(--radius-xl);
    padding: var(--space-12) var(--space-8);
    margin-bottom: var(--space-12);
    min-height: 300px;
    display: flex;
    align-items: center;
  }

  .hero-content { max-width: 700px; color: var(--color-white); }
  .hero h1 { font-size: clamp(32px, 5vw, 48px); color: var(--color-white); margin-bottom: var(--space-4); letter-spacing: -1px; }
  .hero p { font-size: var(--text-lg); opacity: 0.9; line-height: 1.6; max-width: 600px; }

  /* FAQ Styles */
  .faq-container {
    max-width: 800px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .faq-item {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    background: var(--color-white);
    transition: all 0.3s ease;
  }

  .faq-item:hover {
    box-shadow: var(--shadow-sm);
    border-color: var(--color-accent);
  }

  .faq-question {
    width: 100%;
    padding: var(--space-6);
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: none;
    border: none;
    cursor: pointer;
    text-align: left;
    font-size: var(--text-lg);
    font-weight: 600;
    color: var(--color-primary);
    transition: color 0.3s ease;
  }

  .faq-question:hover {
    color: var(--color-accent);
  }

  .faq-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: var(--color-bg-card);
    transition: all 0.3s ease;
    flex-shrink: 0;
    margin-left: var(--space-4);
  }

  .faq-question[aria-expanded="true"] .faq-icon {
    background: var(--color-accent);
    color: var(--color-white);
    transform: rotate(45deg);
  }

  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease-out;
    background: var(--color-bg-card);
  }

  .faq-answer.open {
    max-height: 500px; /* Adjust based on content length */
    transition: max-height 0.5s ease-in;
  }

  .faq-answer-content {
    padding: 0 var(--space-6) var(--space-6) var(--space-6);
    color: var(--color-text-muted);
    line-height: 1.6;
  }

  /* CTA Section */
  .cta {
    background: var(--color-bg-card);
    border-radius: var(--radius-xl);
    padding: var(--space-12) var(--space-8);
    text-align: center;
  }

  .cta-content {
    max-width: 600px;
    margin: 0 auto;
  }

  .cta h2 {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-4);
  }

  .cta p {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    margin-bottom: var(--space-8);
  }

  @media (max-width: 768px) {
    .hero { padding: var(--space-8) var(--space-5); min-height: 250px; }
    .faq-question { padding: var(--space-4); font-size: var(--text-base); }
    .faq-answer-content { padding: 0 var(--space-4) var(--space-4) var(--space-4); }
  }
</style>
